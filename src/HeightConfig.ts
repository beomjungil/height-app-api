/**
 * Configuration for Height API Wrapper
 * @public
 */
export interface HeightConfig {
  /**
   * A secret key to authenticate to Height's API. Starts with `secret_`.
   */
  secretKey: string;
}
